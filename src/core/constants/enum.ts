export enum ROLE {
  ADMIN = 'admin',
  USER = 'user',
}

export enum USER_STATUS {
  ACTIVE = 'active',
  BLOCK = 'BLOCK',
}

export enum PRODUCT_STATUS {
  PENDDING = 'pendding',
  APPROVE = 'approve',
  REJECTED = 'rejected',
}
